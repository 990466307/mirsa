[package]
name = "mirsa-framework"
version = "0.3.0"
edition = "2024"
description = "Generic analysis framework for mirsa"
license = "MIT"
repository = "https://github.com/990466307/mirsa"

[dependencies]
mirsa_core = { package = "mirsa-core", path = "../core", version = "0.3.0" }

[package.metadata.rust-analyzer]
rustc_private = true
