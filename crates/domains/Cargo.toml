[package]
name = "mirsa-domains"
version = "0.2.0"
edition = "2024"
description = "Abstract interpretation domains for mirsa"
license = "MIT"

[dependencies]
mirsa_core = { package = "mirsa-core", path = "../core", version = "0.2.0" }

[package.metadata.rust-analyzer]
rustc_private = true
