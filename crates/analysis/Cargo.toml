[package]
name = "mirsa-analysis"
version = "0.3.0"
edition = "2024"
description = "Analysis orchestration for mirsa"
license = "MIT"
repository = "https://github.com/990466307/mirsa"

[dependencies]
mirsa_core = { package = "mirsa-core", path = "../core", version = "0.3.0" }
mirsa_contracts = { package = "mirsa-contracts", path = "../contracts", version = "0.3.0" }
mirsa_domains = { package = "mirsa-domains", path = "../domains", version = "0.3.0" }
mirsa_framework = { package = "mirsa-framework", path = "../framework", version = "0.3.0" }
mirsa_relations = { package = "mirsa-relations", path = "../relations", version = "0.3.0" }

[package.metadata.rust-analyzer]
rustc_private = true
